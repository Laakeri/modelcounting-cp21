To build:

./setupdev.sh
cd build/Release
make



To use:

cd build/Release

See flags with:
./sharpSAT

Flags in the main experiments:
./sharpSAT -tw -decot 900 -decow 100 -wemod 1 -cs 16000 instance.cnf